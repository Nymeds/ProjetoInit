// Deprecated: chat de tarefa removido. Use o chat de grupo (GroupChatModal). Este arquivo é um stub para evitar erros de importação.


// Deprecated: chat de tarefa removido. Use o chat de grupo (GroupChatModal) em vez disso.
// Este arquivo permanece como stub para evitar erros de importação se houver referências antigas.

export function TaskChatModal() {
  if (typeof window !== 'undefined') console.warn('TaskChatModal is deprecated. Use GroupChatModal instead.');
  return null as any;
}

export default TaskChatModal;
